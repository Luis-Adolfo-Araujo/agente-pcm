# Prompts

Artefatos versionados por finalidade. Cada execução registra nome, versão e hash do
prompt. Prompts não substituem políticas executáveis nem regras de domínio.

Subdiretórios esperados: `system/`, `routing/`, `classifiers/`, `generation/`,
`verification/` e `fallback/`.
