# Dependências da demonstração pública: a API do agente servindo a interface.
#
# Presas às versões verificadas. `psycopg` fica de fora de propósito — a
# demonstração lê um snapshot congelado e nunca abre banco. O extra `pilot`
# (Streamlit) também não entra: a interface publicada é a de `web/`.
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
PyYAML==6.0.3
